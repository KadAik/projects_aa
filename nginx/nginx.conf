# nginx/nginx.conf
worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    sendfile on;

    # Upstream of backend servers (for load balancing if needed)
    upstream backend {
        server back:8000;
    }

    upstream frontend {
        server front:80;
    }

    server {
        listen 80;
        server_name www.psycho.com psycho.com localhost front back;


        # -----------------
        # Serve static files and media (Django) - BEFORE API proxy (locally served)
        # -----------------
        location /static/ {
            alias /staticfiles/;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }

        location /media/ {
            alias /media/;
            expires 7d;
            add_header Cache-Control "public";
        }

        # -----------------
        # Proxy to frontend
        # -----------------
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # -----------------
        # Proxy to Backend
        # -----------------
        location /psycho/api/ {
            proxy_pass http://backend/psycho/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

    }
}
